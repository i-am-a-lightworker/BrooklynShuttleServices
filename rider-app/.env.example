# Copy to .env.local and fill in — never commit the real .env.local file.

# Stripe (use TEST MODE secret key while building — starts with sk_test_)
STRIPE_SECRET_KEY=

# Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Mapbox account > tokens
NEXT_PUBLIC_MAPBOX_TOKEN=
